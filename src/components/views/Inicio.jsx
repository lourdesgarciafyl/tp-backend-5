import {Container, Row} from "react-bootstrap";
import CardReceta from "./receta/CardReceta";


const Inicio = () => {
    return (
        <section className="mainSection">
            <img className="bannerInicio">
            </img>

        </section>
    );
};

export default Inicio;